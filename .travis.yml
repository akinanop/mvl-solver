language: cpp
notifications:
    email: false
compiler:
- clang
- gcc
before_install:
  - python3-pip install --user cpp-coveralls
script:
- cd src
- make
- cd ../playground
- ./Solver -solvenc -file ../benchmarks/Pigeonhole/pgn4
after_success:
  - coveralls --exclude lib --exclude tests --gcov-options '\-lp'
