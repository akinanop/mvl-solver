sudo: required
language: cpp
notifications:
    email: false
compiler:
# - clang
- gcc
install:
#- cd /home/travis/
# install latest LCOV (1.9 was failing for me) [1]
- sudo apt-get install -y lcov
# install lcov to coveralls conversion + upload tool
- gem install coveralls-lcov
script:
- cd src
- make
- cd ../playground
- ./Solver -solvenc -file ../benchmarks/Pigeonhole/pgn4
after_success:
- lcov --compat-libtool --directory . --capture --output-file coverage.info
- coveralls-lcov coverage.info

#- cd /home/travis/
#- lcov-1.11 --directory --capture --output-file coverage.info # capture coverage info
#- lcov-1.11  --remove coverage.info 'tests/*' '/usr/*' --output-file coverage.info # filter out system and test code
#- lcov-1.11  --list coverage.info # debug before upload
#- coveralls-lcov --repo-token ${COVERALLS_TOKEN} coverage.info # uploads to coveralls
